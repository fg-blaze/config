docker inspect OLD_NODE --format "{{json .Spec.Labels}}" > Contents.json && jq -r ".|keys[]" Contents.json | while read key ;
do echo docker node update  --label-add $key="$(jq ".[\"$key\"]" Contents.json) NEW_NODE_NAME";
done
